# URL of the WebSocket server
SERVER_URL=wss://svlan.vivucloud.com

# A unique identifier for this agent. Defaults to the hostname.
# AGENT_ID=my-custom-agent-name

# --- Access Code Configuration ---
# Set to 'true' to generate and save a persistent access code for this agent.
# Set to 'false' to generate a new temporary code every time the agent starts.
PERSISTENT_CODE=true

# The agent will automatically generate and save the access code here if PERSISTENT_CODE is true.
# You can also manually set a code here if you prefer.
# AGENT_ACCESS_CODE=your-predefined-code
